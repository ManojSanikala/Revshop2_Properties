# Application name
spring.application.name=payment

# Server configuration
server.port=8082

# Database configuration (Local MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/payment
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show=true
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Eureka Client configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.instance.preferIpAddress=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Razorpay API keys (replace with your actual keys)
razorpay.key=rzp_test_KDLqcPjMljFnI2
razorpay.secret=rnyhJhZX18mlebgsMSDzJV5E

# Application type (for non-blocking)
spring.main.web-application-type=reactive
